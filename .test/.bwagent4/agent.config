root: "${HOME}/development/bw/.bwagent4"
keepN: 3
tlsconfig:
  servername: "wambli.talla.io"
  cert: "${HOME}/.config/bearded-wookie/default/tlsserver.cert"
  key: "${HOME}/.config/bearded-wookie/default/tlsserver.key"
  ca: "${HOME}/.config/bearded-wookie/default/tlsca.cert"
storage:
  backend: s3
  options:
    bucket: example
    key_prefix: foo
